# Ejercicios Modulo 3

Breve descripción: propósito y alcance del proyecto Python.

## Estado
1. Estado: En desarrollo / Estable
2. Lenguaje: Python

## Requisitos
1. Python 3.10+ (o la versión que uses)
2. Dependencias en `requirements.txt`

## Instalación
1. Clone el repositorio:
```bash
git clone https://github.com/usuario/repo.git
cd repo
python -m venv .venv
.\.venv\Scripts\activatepip install -r requirements.txtpython -m src.mainpython -m src.main --input data/input.csv --output results/pytestpytest --cov=srcblack .flake8